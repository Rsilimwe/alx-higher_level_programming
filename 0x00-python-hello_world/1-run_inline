#!/bin/bash

# Check if the environment variable PYCODE is set
if [ -z "$PYCODE" ]; then
    echo "Error: PYCODE environment variable is not set."
    exit 1
fi
# Run the Python code using '<<<' to pass the content of PYCODE
python3 <<< "$PYCODE"
